#
# Identity Security Cloud V2025 API
# Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
# Version: v2025
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

An example of the JSON payload that will be sent by the trigger to the subscribed service.

.PARAMETER Json

JSON object

.OUTPUTS

TriggerExampleInput<PSCustomObject>
#>
function ConvertFrom-V2025JsonToTriggerExampleInput {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match AccessRequestDynamicApprover defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccessRequestDynamicApprover $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccessRequestDynamicApprover"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccessRequestDynamicApprover' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccessRequestPostApproval defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccessRequestPostApproval $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccessRequestPostApproval"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccessRequestPostApproval' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccessRequestPreApproval defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccessRequestPreApproval $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccessRequestPreApproval"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccessRequestPreApproval' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccountAggregationCompleted defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccountAggregationCompleted $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountAggregationCompleted"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountAggregationCompleted' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccountAttributesChanged defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccountAttributesChanged $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountAttributesChanged"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountAttributesChanged' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccountCorrelated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccountCorrelated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountCorrelated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountCorrelated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccountUncorrelated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccountUncorrelated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountUncorrelated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountUncorrelated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match AccountsCollectedForAggregation defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToAccountsCollectedForAggregation $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountsCollectedForAggregation"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountsCollectedForAggregation' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match CampaignActivated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToCampaignActivated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CampaignActivated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CampaignActivated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match CampaignEnded defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToCampaignEnded $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CampaignEnded"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CampaignEnded' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match CampaignGenerated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToCampaignGenerated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CampaignGenerated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CampaignGenerated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match CertificationSignedOff defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToCertificationSignedOff $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CertificationSignedOff"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CertificationSignedOff' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match IdentityAttributesChanged defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToIdentityAttributesChanged $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IdentityAttributesChanged"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IdentityAttributesChanged' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match IdentityCreated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToIdentityCreated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IdentityCreated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IdentityCreated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match IdentityDeleted defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToIdentityDeleted $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IdentityDeleted"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IdentityDeleted' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match ProvisioningCompleted defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToProvisioningCompleted $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "ProvisioningCompleted"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'ProvisioningCompleted' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SavedSearchComplete defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSavedSearchComplete $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SavedSearchComplete"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SavedSearchComplete' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceAccountCreated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceAccountCreated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceAccountCreated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceAccountCreated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceAccountDeleted defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceAccountDeleted $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceAccountDeleted"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceAccountDeleted' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceAccountUpdated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceAccountUpdated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceAccountUpdated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceAccountUpdated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceCreated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceCreated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceCreated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceCreated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceDeleted defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceDeleted $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceDeleted"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceDeleted' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match SourceUpdated defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToSourceUpdated $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "SourceUpdated"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'SourceUpdated' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        # try to match VAClusterStatusChangeEvent defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2025JsonToVAClusterStatusChangeEvent $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "VAClusterStatusChangeEvent"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'VAClusterStatusChangeEvent' defined in oneOf (V2025TriggerExampleInput). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([AccessRequestDynamicApprover, AccessRequestPostApproval, AccessRequestPreApproval, AccountAggregationCompleted, AccountAttributesChanged, AccountCorrelated, AccountUncorrelated, AccountsCollectedForAggregation, CampaignActivated, CampaignEnded, CampaignGenerated, CertificationSignedOff, IdentityAttributesChanged, IdentityCreated, IdentityDeleted, ProvisioningCompleted, SavedSearchComplete, SourceAccountCreated, SourceAccountDeleted, SourceAccountUpdated, SourceCreated, SourceDeleted, SourceUpdated, VAClusterStatusChangeEvent]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("AccessRequestDynamicApprover", "AccessRequestPostApproval", "AccessRequestPreApproval", "AccountAggregationCompleted", "AccountAttributesChanged", "AccountCorrelated", "AccountUncorrelated", "AccountsCollectedForAggregation", "CampaignActivated", "CampaignEnded", "CampaignGenerated", "CertificationSignedOff", "IdentityAttributesChanged", "IdentityCreated", "IdentityDeleted", "ProvisioningCompleted", "SavedSearchComplete", "SourceAccountCreated", "SourceAccountDeleted", "SourceAccountUpdated", "SourceCreated", "SourceDeleted", "SourceUpdated", "VAClusterStatusChangeEvent")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([AccessRequestDynamicApprover, AccessRequestPostApproval, AccessRequestPreApproval, AccountAggregationCompleted, AccountAttributesChanged, AccountCorrelated, AccountUncorrelated, AccountsCollectedForAggregation, CampaignActivated, CampaignEnded, CampaignGenerated, CertificationSignedOff, IdentityAttributesChanged, IdentityCreated, IdentityDeleted, ProvisioningCompleted, SavedSearchComplete, SourceAccountCreated, SourceAccountDeleted, SourceAccountUpdated, SourceCreated, SourceDeleted, SourceUpdated, VAClusterStatusChangeEvent]). JSON Payload: $($Json)"
        }
    }
}

