#
# Identity Security Cloud API - Suggested Entitlement Description
# Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
# Version: v1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Describes the target assignee for entitlement recommendations.

.PARAMETER Json

JSON object

.OUTPUTS

EntitlementRecommendationAssignee<PSCustomObject>
#>
function ConvertFrom-JsonToEntitlementRecommendationAssignee {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match EntitlementRecommendationAssigneeOneOf defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToEntitlementRecommendationAssigneeOneOf $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "EntitlementRecommendationAssigneeOneOf"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'EntitlementRecommendationAssigneeOneOf' defined in oneOf (EntitlementRecommendationAssignee). Proceeding to the next one if any."
        }

        # try to match EntitlementRecommendationAssigneeOneOf1 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToEntitlementRecommendationAssigneeOneOf1 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "EntitlementRecommendationAssigneeOneOf1"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'EntitlementRecommendationAssigneeOneOf1' defined in oneOf (EntitlementRecommendationAssignee). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([EntitlementRecommendationAssigneeOneOf, EntitlementRecommendationAssigneeOneOf1]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("EntitlementRecommendationAssigneeOneOf", "EntitlementRecommendationAssigneeOneOf1")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([EntitlementRecommendationAssigneeOneOf, EntitlementRecommendationAssigneeOneOf1]). JSON Payload: $($Json)"
        }
    }
}

