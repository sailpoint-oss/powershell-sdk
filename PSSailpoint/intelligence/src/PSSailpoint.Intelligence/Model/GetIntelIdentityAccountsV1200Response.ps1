#
# Identity Security Cloud API - Intelligence
# Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
# Version: v1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

GetIntelIdentityAccountsV1200Response<PSCustomObject>
#>
function ConvertFrom-JsonToGetIntelIdentityAccountsV1200Response {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match IntelAccountsSlice defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntelAccountsSlice $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IntelAccountsSlice"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IntelAccountsSlice' defined in oneOf (GetIntelIdentityAccountsV1200Response). Proceeding to the next one if any."
        }

        # try to match Intelmachineaccountwire[] defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIntelmachineaccountwire[] $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "Intelmachineaccountwire[]"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'Intelmachineaccountwire[]' defined in oneOf (GetIntelIdentityAccountsV1200Response). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([IntelAccountsSlice, Intelmachineaccountwire[]]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("IntelAccountsSlice", "Intelmachineaccountwire[]")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([IntelAccountsSlice, Intelmachineaccountwire[]]). JSON Payload: $($Json)"
        }
    }
}

