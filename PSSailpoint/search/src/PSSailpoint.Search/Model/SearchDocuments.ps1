#
# Identity Security Cloud API - Search
# Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
# Version: v1
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

SearchDocuments<PSCustomObject>
#>
function ConvertFrom-JsonToSearchDocuments {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match AccessProfileDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAccessProfileDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccessProfileDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccessProfileDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        # try to match AccountActivityDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAccountActivityDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AccountActivityDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AccountActivityDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        # try to match EntitlementDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToEntitlementDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "EntitlementDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'EntitlementDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        # try to match EventDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToEventDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "EventDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'EventDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        # try to match IdentityDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIdentityDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IdentityDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IdentityDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        # try to match RoleDocuments defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToRoleDocuments $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "RoleDocuments"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'RoleDocuments' defined in oneOf (SearchDocuments). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([AccessProfileDocuments, AccountActivityDocuments, EntitlementDocuments, EventDocuments, IdentityDocuments, RoleDocuments]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("AccessProfileDocuments", "AccountActivityDocuments", "EntitlementDocuments", "EventDocuments", "IdentityDocuments", "RoleDocuments")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([AccessProfileDocuments, AccountActivityDocuments, EntitlementDocuments, EventDocuments, IdentityDocuments, RoleDocuments]). JSON Payload: $($Json)"
        }
    }
}

