#
# Identity Security Cloud v2026 API
# Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.
# Version: v2026
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

GetStream200Response<PSCustomObject>
#>
function ConvertFrom-V2026JsonToGetStream200Response {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match StreamConfigResponse defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2026JsonToStreamConfigResponse $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "StreamConfigResponse"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'StreamConfigResponse' defined in oneOf (V2026GetStream200Response). Proceeding to the next one if any."
        }

        # try to match StreamConfigResponse[] defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-V2026JsonToStreamConfigResponse[] $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "StreamConfigResponse[]"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'StreamConfigResponse[]' defined in oneOf (V2026GetStream200Response). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([StreamConfigResponse, StreamConfigResponse[]]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("StreamConfigResponse", "StreamConfigResponse[]")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([StreamConfigResponse, StreamConfigResponse[]]). JSON Payload: $($Json)"
        }
    }
}

